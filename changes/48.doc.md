The compat page and the base85 page correct three numbers: which flags the drop-in reads once, how often a cut base85 string decodes by accident, and that base85 text needs no escaping inside a JSON string.
