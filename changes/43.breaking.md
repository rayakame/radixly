`Codec.bits_per_char` and every codec's `BITS_PER_CHAR` are floats, `6.4` for base85 and z85 and whole numbers as before for the rest; code that compares them with `==` keeps working, code that requires `int` (an annotation, `isinstance`, a bit shift) does not.
