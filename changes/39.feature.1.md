`radixly.compat.base64`: `b64encode`, `b64decode`, `standard_b64encode`, `standard_b64decode`, `urlsafe_b64encode` and `urlsafe_b64decode` run in C, `altchars` and `validate` included; `b64decode` follows the running interpreter's `binascii.a2b_base64`, whose treatment of stray padding changed in CPython 3.12.4, 3.13.13 and 3.14.4.
