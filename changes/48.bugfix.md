A `wrapcol` whose `__index__` disagrees with its comparison no longer makes `a85encode` fail without an error, and `a85decode` asks a `bytes` subclass its own `__contains__` for `ignorechars`, as the standard library does.
