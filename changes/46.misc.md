Every C entry point is called in a loop in the test suite, on its error paths too, and must give back the memory it allocated; a leak that CPython's own allocator noise hid from the sanitizers now fails a test.
