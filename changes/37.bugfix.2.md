`radixly.compat.base64` decoders dispatch on `isinstance(s, str)` like the standard library, so a `str` subclass with its own `encode` and a lazy proxy whose class is `str` now decode the same way; where the standard library writes `raise ... from None`, the port hides its exception context too.
