`DecodeError` keeps notes and attributes through `pickle`, `copy.copy` and `copy.deepcopy`; previously `add_note` and any attribute set on the exception were dropped, and a non-string first argument made unpickling fail.
