On Python 3.11, importing `radixly._core` into a subinterpreter is refused with the same `ImportError` that 3.12 and later raise, instead of quietly replacing the cached `binascii.Error` of the main interpreter.
