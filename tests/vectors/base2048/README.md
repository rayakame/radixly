# Base2048 conformance vectors

`pairs/` and `bad/` are vendored unchanged from the `test-data` directory of
qntm's base2048 (<https://github.com/qntm/base2048>, version 3.0.1, commit
f9b99dd), MIT licensed, see `LICENSE.txt`. `.txt` files are UTF-8 with no
trailing newline, see `.gitattributes`.
