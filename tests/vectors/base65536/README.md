# Base65536 conformance vectors

`pairs/` and `bad/` are vendored unchanged from qntm's base65536-test
(<https://github.com/qntm/base65536-test>, commit 3e44c8d), MIT licensed,
see `LICENSE.txt`. `.txt` files are UTF-8 with no trailing newline, see
`.gitattributes`.
